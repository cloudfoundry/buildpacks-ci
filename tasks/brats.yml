---
platform: linux
image_resource:
  type: docker-image
  source:
    repository: cfbuildpacks/ci
inputs:
  - name: buildpacks-ci
  - name: brats
  - name: cf-environments
run:
  path: buildpacks-ci/scripts/brats
params:
  RUBYGEM_MIRROR:
