---
platform: linux
image_resource:
  type: docker-image
  source:
    repository: cfbuildpacks/ci

inputs:
  - name: buildpacks-ci
  - name: repo
output:
  - name: release-candidate

run:
  path: buildpacks-ci/tasks/build-cnb-buildpack/run.sh
