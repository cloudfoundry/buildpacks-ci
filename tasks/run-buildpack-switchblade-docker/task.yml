---
platform: linux
image_resource:
  type: docker-image
  source:
    repository: cfbuildpacks/ci
    username: ((coredeps-dockerhub-user.username))
    password: ((coredeps-dockerhub-user.password))
inputs:
  - name: buildpacks-ci
  - name: buildpack
run:
  path: buildpacks-ci/tasks/run-buildpack-switchblade-docker/run.sh
params:
  CF_STACK:
  GITHUB_TOKEN:
  GINKGO_ATTEMPTS:
  GINKGO_NODES:
  COMPOSER_GITHUB_OAUTH_TOKEN:
