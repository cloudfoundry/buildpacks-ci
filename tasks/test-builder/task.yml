---
platform: linux
image_resource:
  type: docker-image
  source:
    repository: cfbuildpacks/ci
inputs:
  - name: buildpacks-ci
  - name: pack
  - name: builder-image

run:
  path: buildpacks-ci/tasks/test-builder/run.sh

params:
  BUILDER_REPO:
  STACK:
