---
platform: linux
image_resource:
  type: docker-image
  source:
    repository: cfbuildpacks/ci
inputs:
  - name: bbl-state
  - name: cf-deployment
  - name: buildpacks-ci

run:
  path: buildpacks-ci/tasks/use-bosh-lite-cloud-config/run.sh

params:
  ENV_NAME:
