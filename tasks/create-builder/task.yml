---
platform: linux
image_resource:
  type: docker-image
  source:
    repository: cfbuildpacks/ci
inputs:
  - name: buildpacks-ci
  - name: sources
  - name: version
  - name: pack
outputs:
  - name: builder-image

run:
  path: buildpacks-ci/tasks/create-builder/run.rb

params:
  BUILDER_REPO:
  BUILD_IMAGE:
  RUN_IMAGE:
  STACK:
