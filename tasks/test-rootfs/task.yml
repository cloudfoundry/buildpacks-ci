---
platform: linux
image_resource:
  type: docker-image
  source:
    repository: cfbuildpacks/ci
inputs:
  - name: buildpacks-ci
  - name: rootfs
  - name: version
  - name: rootfs-artifacts
run:
  path: buildpacks-ci/tasks/test-rootfs/run.sh
params:
  STACK:
