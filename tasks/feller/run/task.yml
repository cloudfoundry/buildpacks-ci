---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: cfbuildpacks/ci

inputs:
- name: buildpacks-ci
- name: feller

run:
  path: bash
  args:
  - -cl
  - buildpacks-ci/tasks/feller/run/task.sh

params:
  TRACKER_PROJECT:
  TRACKER_TOKEN:
  GITHUB_TOKEN:
