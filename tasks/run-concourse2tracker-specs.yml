---
platform: linux
image_resource:
  type: docker-image
  source:
    repository: cfbuildpacks/ci
inputs:
  - name: buildpacks-ci
run:
  path: bash
  args:
    - -c
    - |
      cd buildpacks-ci/lib/concourse2tracker-resource
      bundle
      bundle exec rspec
