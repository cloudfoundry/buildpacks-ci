#!/usr/bin/env ruby
# encoding: utf-8

# Normalize YAML for comparison by:
# 1. Parsing and re-dumping to normalize formatting
# 2. This removes extra whitespace, comments, and formatting differences

require 'yaml'

input = ARGF.read
parsed = YAML.load(input)
puts YAML.dump(parsed)
