#!/usr/bin/env bash

set -e

base_dir=$(pwd)

pushd buildpacks-ci/scripts/notifications
  bundle install
  ./new-releases $base_dir/new-releases | tee updates | ./slack
popd

pushd new-releases
  git add -A
  git ci -m 'Updates for latest tags'
popd
