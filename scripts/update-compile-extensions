#!/usr/bin/env bash

set -e

cd buildpack/compile-extensions
git pull origin master
bundle install
bundle exec rspec
cd ..

git config --global user.email "ci@localhost"
git config --global user.name "CI Bot"
git add compile-extensions/
git commit -m "Update compile-extensions"
